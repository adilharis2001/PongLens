# Storage section without the paragraph: build 206

Released September 14, 2026, on top of build 205 (see
`2026-09-14-storage-everything-counts-build-205.md`).

- Adil, after seeing build 205 on his phone: the explanatory paragraph
  under the storage breakdown ("Everything you store counts: match videos
  and their cut versions…") repeats what the list above it already says.
  Removed on iOS and on the web Account page, so the section is the
  number, the bar, the breakdown and the request row.
- Nothing else changed in the app.
- Verified: iOS Debug build for the simulator, full Next.js production
  build, the web section linted.

Archive: `/tmp/PongLens-storage-1.0-206.xcarchive`.
Build log: `/tmp/ponglens-storage-206-archive.log`.
Upload log: `/tmp/ponglens-storage-206-export.log`.
