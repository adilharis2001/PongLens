# Reviewed split test release

Separate a completed low-bounce/net episode from a later verified serve within an existing body card. Enable only on explicitly selected Julian test jobs, preserving scored originals and earlier test copies. Record applied rule version and each split in processing evidence; ordinary jobs retain the existing end-only policy.

| Step | Check |
|---|---|
| Add failing algorithm tests | Restart preserved, multiple restarts, live crossing veto, no-card loss, independent winner records |
| Implement opt-in processing | Existing net evidence OR three low bounces; same reviewed longer ending and 1.6s serve lead; minimum card length; no broad quiet/reset rules |
| Job-to-pipeline option | Literal true only; absent/false preserve baseline; applied provenance distinguishes requested from used |
| Frozen replay | Seven complete recordings and two fresh Julian caches; owner controls; exact baseline when disabled |
| Package | Worker tests, real isolated npm build, fixed source commit, sealed package integrity and offline inference checks |
| Activate and rerun | Drain main/fast; signed launcher backups; exact new release pulses; three fresh owner copies with explicit option; verify published counts, video and private winner attempts |

No UI change, new execution lane, score mutation, or model fitting. No broad continuation threshold relaxation. The ordinary pipeline's existing rally continuation joins stay in force.
